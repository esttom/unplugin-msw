{
  "private": true,
  "scripts": {
    "dev": "nodemon -w '../src/**/*.ts' -e .ts -x vite",
    "build": "vite build",
    "serve": "nodemon -w '../src/**/*.ts' -e .ts -x vite preview"
  },
  "msw": {
    "workerDirectory": "public"
  }
}
